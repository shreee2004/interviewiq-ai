/**
 * User profile and preferences — distinct from {@link com.interviewiq.auth}, which owns
 * core identity ({@code users}). Owns {@code user_profiles} and {@code user_preferences}
 * (docs/DATABASE.md §2.3). See docs/API_DESIGN.md §3.
 */
package com.interviewiq.user;
