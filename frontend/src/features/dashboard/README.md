# dashboard

Stat tiles, skill radar, weekly progress, recent interviews, leaderboard.
Backed by `GET /analytics/dashboard` etc. — `docs/API_DESIGN.md` §8. Screen
layout: `docs/UI_WIREFRAMES.md` §2.2. Currently rendered by
`PlaceholderPage` via `src/app/router.tsx`.
