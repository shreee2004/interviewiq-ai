# admin

User management, platform analytics, AI cost/usage, feature flags, log
search. Requires `ROLE_ADMIN` — backed by `/admin/*`, `docs/API_DESIGN.md`
§11. Gate this route behind an admin check once auth exists.
