# interview/session

The core product screen: live question/answer loop over the WebSocket
protocol in `docs/API_DESIGN.md` §5.1, voice/text answer input, live signal
panel (confidence/pace/filler words), transcript timeline. Screen layout:
`docs/UI_WIREFRAMES.md` §2.4. Expect a `useInterviewSocket()` hook here once
built (see `docs/ARCHITECTURE.md` §4 component hierarchy).
