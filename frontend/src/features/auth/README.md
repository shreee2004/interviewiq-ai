# auth

Login, register, forgot/reset password, Google OAuth, email verification, 2FA.
Talks to the `/auth/*` endpoints in `docs/API_DESIGN.md` §2. Not implemented
yet — see `docs/ARCHITECTURE.md` §4 for the `api.ts` / `components/` /
`types.ts` convention this folder follows once it is.
